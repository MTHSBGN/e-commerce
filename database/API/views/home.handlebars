{{> header @root}}
<div id="cards-container">
    {{#each products}}
    <a href="/product/{{product_id}}" class="product_link">
        <div class="card">
            <img src="images/products/{{main_image}}">
            <div class="card-info">
                <p>{{name}}</p>
                <p>{{price}} €</p>
            </div>
        </div>
    </a>
    {{/each}}
</div>
